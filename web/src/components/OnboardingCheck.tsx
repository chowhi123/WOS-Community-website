"use client";

export default function OnboardingCheck() {
    // 🔒 Onboarding redirection is now securely handled globally by middleware.ts
    // This component remains as a passive shell to avoid breaking layout.tsx imports.
    return null;
}
